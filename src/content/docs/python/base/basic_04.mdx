---
title: 基本(標準ライブラリ コレクション)
description: 基本(標準ライブラリ コレクション)
tags:
  - "Python"
sidebar:
  # このリンクのカスタムラベルを設定します
  label: 基本(標準ライブラリ コレクション)
  # このリンクの順番をカスタマイズします（数字が小さいほど上に表示されます）
  order: 40
  # このリンクにバッジを追加します
  # badge:
  #     text: ""
  #     variant: tip
---

import { LinkCard, Tabs, TabItem, Steps } from "@astrojs/starlight/components";
import LinkPreview from "@components/LinkPreview.astro";
import MermaidChart from "@components/MermaidChart.astro";
import Table from "@components/Table.astro";

---

## シーケンス型

複数の値を持つものを総称してコレクション、コンテナーと呼びます。

<Table
  caption=""
  colWidth={["10rem"]}
  columns={["分類", "型", "説明"]}
  data={[
    [
      "シーケンス型",
      ["リスト", "タプル", "range"].join("<br />"),
      ["インデックスで管理", "値の重複は OK"].join("<br />"),
    ],
    ["セット(集合)型", ["set", "frozenset"].join("<br />"), ["順番は持たない", "値の重複は不可"].join("<br />")],
    ["辞書(マッピング)型", ["dict"].join("<br />"), ["キーと値のペアで管理", "キーの重複は不可"].join("<br />")],
  ]}
/>

### リストの作成

list を生成するには

- list リテラル
- リスト内包表記
- list コンストラクター
- リストを返す関数/メソッド

[go コマンドについて](/golang/cmd/index)

### リストから特定範囲の要素を取得する--スライス構文

### リストの要素数を取得する

### リストに要素を追加・削除する

### リスト内の要素を削除する

### 複数要素を追加/置換/削除する

### リストを検索する

### リストを複製する

### リストを連結する

### リストの内容を並べ替える

### リストを for ループで処理する方法

### リスト内の要素が True であるかを判定するには、

### リスト内の要素を加工する

### リストの内容を特定の条件で絞り込む

### リスト内の要素を順に処理して一つにまとめる

### キュー構造を実装する

### イミュータブルなリストを作成する

## セット型

## 辞書(dict)型
